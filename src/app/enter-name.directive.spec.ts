import { EnterNameDirective } from './enter-name.directive';

describe('EnterNameDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterNameDirective();
    expect(directive).toBeTruthy();
  });
});
