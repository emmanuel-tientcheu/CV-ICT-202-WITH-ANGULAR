import { DisplayAddDirective } from './display-add.directive';

describe('DisplayAddDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayAddDirective();
    expect(directive).toBeTruthy();
  });
});
