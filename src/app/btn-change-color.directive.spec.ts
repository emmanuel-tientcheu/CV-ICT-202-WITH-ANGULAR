import { BtnChangeColorDirective } from './btn-change-color.directive';

describe('BtnChangeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnChangeColorDirective();
    expect(directive).toBeTruthy();
  });
});
